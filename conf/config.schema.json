{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "type": "object",
  "properties": {
    "rootDir": {
      "description": "Path to the root folder",
      "type": "string"
    },
    "tempDir": {
      "description": "Path to the temporary folder",
      "type": "string"
    }
  },
  "required": ["rootDir", "tempDir"]
}
